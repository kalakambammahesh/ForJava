import java.util.Set;
import java.util.HashSet;
import java.util.Iterator;

public class TestHashSet{
    public static void main(String[] args){
        
        Set<String> obj = new HashSet<String>();
        
        obj.add("A");
        obj.add("B");
        obj.add("C");
        obj.add("A");
        
        Iterator itr = obj.iterator();
        
        while(itr.hasNext()){
            System.out.println("Items:" + itr.next());
        }
    }
}

//HashSet class implements Set Interface. It represents the collection that uses a hash table for storage.
Hashing is used to store the elements in the HashSet. It contains the unique items. Utmost contains only one null value....
